# Core dependencies
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# Optional but recommended
accelerate>=0.20.0  # For faster model loading
datasets>=2.12.0    # If you want to process datasets
scikit-learn>=1.3.0 # For additional metrics
